import React from 'react';
import ReactDOM from 'react-dom/client';

const App = () => {
  return (
    <div>App.jsx!</div>
  );
}

ReactDOM.createRoot(
  document.getElementById('root'),
).render(<App />);
